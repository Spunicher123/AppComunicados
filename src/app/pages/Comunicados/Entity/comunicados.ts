export interface comunicados{
    id: string;
    titulo: string;
    descripcion: string;
    link: string;
}

export interface comunicadosGA{
    id?: string;
    titulo: string;
    descripcion: string;
    link: string;
}