
export interface cliente{
    id: string;
    nombre: string;
    telefono: string;
    email: string;
}

export interface clienteGA{
    id?: string;
    nombre: string;
    telefono: string;
    email: string;
}